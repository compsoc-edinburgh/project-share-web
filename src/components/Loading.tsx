/** The one loading state on the site: the word, and dots that count up. */
const Loading = () => (
  <p className="loading" role="status">
    loading<span aria-hidden="true">...</span>
  </p>
)

export default Loading
